# Superseded by the improved discrete-safe run

Use `planning_v3_discrete_safe` for reporting. This directory retains the
pre-improvement ablation that exposed finite-step safety and slow grid search.
